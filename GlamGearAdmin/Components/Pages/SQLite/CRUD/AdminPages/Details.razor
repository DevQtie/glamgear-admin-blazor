@page "/sqlite/crud/admin_pages/details"
@using GlamGearAdmin.Models.SQLite
@using Microsoft.EntityFrameworkCore
@using GlamGearAdmin.Models
@inject IDbContextFactory<GlamGearAdmin.Data.SQLite.BlazorWebAppAdminContext> DbFactory
@inject NavigationManager NavigationManager

<PageTitle>Details</PageTitle>

<h1>Details</h1>

<div>
    <h2>Admin</h2>
    <hr />
    @if (admin is null)
    {
        <div class="position-absolute top-50 start-50 translate-middle">
            <div class="spinner-grow text-danger spinner-grow-xxl" role="status">
                <span class="visually-hidden">Loading...</span>
            </div>
        </div>
    }
    else
    {
        <dl class="row">
            <dt class="col-sm-2">User ID</dt>
            <dd class="col-sm-10">@admin.UserID</dd>
            <dt class="col-sm-2">Username</dt>
            <dd class="col-sm-10">@admin.Username</dd>
            <dt class="col-sm-2">Given Name</dt>
            <dd class="col-sm-10">@admin.GivenName</dd>
            <dt class="col-sm-2">Middle Name</dt>
            <dd class="col-sm-10">@admin.MiddleName</dd>
            <dt class="col-sm-2">Family Name</dt>
            <dd class="col-sm-10">@admin.FamilyName</dd>
            <dt class="col-sm-2">Email Add</dt>
            <dd class="col-sm-10">@admin.EmailAdd</dd>
            <dt class="col-sm-2">Role Type</dt>
            <dd class="col-sm-10">@admin.RoleType</dd>
        </dl>
        <div>
            <a href="@($"/sqlite/crud/admin_pages/edit?userid={admin.UserID}")">Edit</a> |
            <a href="@($"/sqlite/crud/admin_pages")">Back to List</a>
        </div>
    }
</div>

@code {
    private Admin? admin;

    [SupplyParameterFromQuery]
    private string? UserID { get; set; }

    protected override async Task OnInitializedAsync()
    {
        using var context = DbFactory.CreateDbContext();
        admin = await context.Admin.FirstOrDefaultAsync(m => m.UserID == UserID);

        if (admin is null)
        {
            NavigationManager.NavigateTo("notfound");
        }
    }
}
